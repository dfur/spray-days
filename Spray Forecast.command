#!/usr/bin/env bash
#
# Spray Forecast — opens the app. The page fetches and scores the forecast
# itself (see index.html), so there is nothing to build any more.
# Double-click this file any time you want the report.
#
cd "$(dirname "$0")"
open "index.html"
